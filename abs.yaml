build_parameters:
  - "--suppress-variables"
  - "--error-overlinking"
  - "--error-overdepending"
  - "--no-test"

upload_channels:
  - ad-testing/label/py315

# Resolve python 3.15.0rc1 from python-feedstock PR #244 staging (not yet on testing).
channels:
  - https://staging.continuum.io/pbp/fs/python-feedstock/pr244/e3ff93e

build_env_vars:
  ANACONDA_ROCKET_ENABLE_PY315: yes
